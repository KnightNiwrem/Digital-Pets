/**
 * Centralized constants for the exploration system.
 */

// =============================================================================
// Exploration XP
// =============================================================================

/**
 * Base XP granted per exploration activity completion.
 * This is multiplied by the activity's skillFactors to determine
 * the actual XP gained for each skill.
 */
export const BASE_EXPLORATION_XP = 15;

// =============================================================================
// Encounter System
// =============================================================================

/**
 * Default maximum level for wild encounters when location doesn't specify one.
 */
export const DEFAULT_ENCOUNTER_MAX_LEVEL = 10;
